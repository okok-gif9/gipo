# یادداشت اعتبارسنجی انتشار گیپو

## مشاهده

پس از ثبت نسخهٔ `02d990f8`، اولین بازبینی دامنهٔ عمومی در **2026-08-17 09:43 GMT+3:30** همچنان محتوای ساخت قبلی را نشان داد. در بازبینی مجدد در **09:50**، همان دامنه عنوان، لوگوی نوشتاری و متن `GIPO` نسخهٔ جدید را نمایش داد. این توالی با **انتشار تدریجی یا کش موقت در لایهٔ انتشار** سازگار است؛ هیچ خطای ساخت، خطای نوع یا علت وابسته به کد برنامه مشاهده نشد.

## نشانگر راستی‌آزمایی

برنامه اکنون نشانگر کد-محور `GIPO-2026.08.17` را در نمای عمومیِ پیش از ورود نشان می‌دهد. پس از هر نسخهٔ جدید، مشاهدهٔ این نشانگر در دامنهٔ عمومی تأیید می‌کند که همان ساخت اخیر به دامنه رسیده است.

## نتیجه

مورد «نسخهٔ قدیمی در دامنه» پس از انتشار جدید برطرف شد. نوار هشدار وضعیت پرداخت که در بالای دامنه دیده می‌شود، بیرون از کنترل کد پروژه است و برای آزمون واقعی Grok و تلگرام باید از مسیر پشتیبانی پلتفرم برطرف شود.

## پیگیری نشانگر نسخه

پس از ثبت نسخهٔ دارای نشانگر در **09:54**، دو بازبینی پشت‌سرهم از دامنهٔ عمومی همچنان نسخهٔ گیپو را بدون متن `GIPO-2026.08.17` برگرداندند. این نتیجه نشان می‌دهد که انتشار دامنه هنوز به ساختِ نشانگر‌دار نرسیده است؛ در نتیجه اعتبارسنجی نهایی نشانگر تا پایان انتشار تدریجی باز می‌ماند.
